# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your-secret-key-here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Google Gemini AI (Opcional - server-only, NÃO expor no client)
GEMINI_API_KEY=your-gemini-api-key-here

# News API (Opcional - para notícias reais no modo stealth)
NEXT_PUBLIC_NEWS_API_KEY=your-news-api-key-here

# Push disfarçado de notícia (Sugestão 3) - gerar com: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# Stripe Webhook (Opcional - necessário para processar eventos do Stripe)
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# Node Environment
NODE_ENV=development

# Vercel Cron - use: openssl rand -hex 32
# A Vercel envia automaticamente: Authorization: Bearer <CRON_SECRET>
CRON_SECRET=your-cron-secret-for-alerts

# Supabase Service Role (para Cron e operações server-side)
# Pegue em: Supabase → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Sentry (plano gratuito: 5.000 erros/mês)
# 1. Crie conta em https://sentry.io/signup/
# 2. Novo projeto → Next.js
# 3. Copie o DSN e cole abaixo
NEXT_PUBLIC_SENTRY_DSN=

# Opcional: para upload de source maps (stack traces legíveis)
# Crie em: Sentry → Settings → Auth Tokens
# SENTRY_ORG=seu-org-slug
# SENTRY_PROJECT=seu-projeto-slug
# SENTRY_AUTH_TOKEN=sntrys_xxx
